const INITIAL_STATE = {
  appTitle: "MotoSafe",
  loadingText: "Acessando dados ...",
  errorMsgText: "Verificando ...",
};

export default function defaultReducer(state = INITIAL_STATE) {
  return state;
}
